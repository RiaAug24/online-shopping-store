module.exports = {
  content: [
    './views/**/*.ejs',  // This will include all .ejs files in the views directory and its subdirectories
    './public/styles/*.css',  // Optional: Include your CSS files if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
